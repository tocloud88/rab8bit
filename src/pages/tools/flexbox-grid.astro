---
import Layout from '../../layouts/Layout.astro';
import AdBanner from '../../components/ads/AdBanner.astro';
import FlexboxGridVisualizer from '../../components/tools/FlexboxGridVisualizer';
---

<Layout 
  title="CSS Flexbox &amp; Grid 인터랙티브 시각화 | rab8bit 스마트 툴스" 
  description="justify-content, align-items, flex-direction 등 Flex/Grid 속성을 슬라이더로 조작하며 동작 원리를 배웁니다."
>
  <div class="space-y-8 max-w-4xl mx-auto py-4">

    <div class="text-center space-y-3">
      <div class="inline-flex items-center gap-2 px-4 py-1 rounded-full stitch-pill text-xs font-bold">
        <span>📦</span> <span>인터랙티브</span>
      </div>
      <h1 class="text-2xl sm:text-4xl font-extrabold text-white tracking-tight">
        CSS Flexbox &amp; Grid 인터랙티브 시각화
      </h1>
      <p class="text-slate-400 max-w-xl mx-auto text-xs sm:text-sm leading-relaxed">
        justify-content, align-items, flex-direction 등 Flex/Grid 속성을 슬라이더로 조작하며 동작 원리를 배웁니다.
      </p>
    </div>

    <AdBanner type="leaderboard" label="스마트 비즈니스 &amp; 생산성 스폰서" />

    <FlexboxGridVisualizer client:load />

    <AdBanner type="horizontal" className="my-8" />
  </div>
</Layout>
