---
import Layout from '../../layouts/Layout.astro';
import AdBanner from '../../components/ads/AdBanner.astro';
import MarkdownPreviewTool from '../../components/tools/MarkdownPreviewTool';
---

<Layout 
  title="마크다운(Markdown) 실시간 에디터 | rab8bit 스마트 툴스" 
  description="마크다운 문법으로 작성된 텍스트를 실시간으로 서식화된 HTML 뷰로 미리보고 HTML 코드를 복사합니다."
>
  <div class="space-y-8 max-w-4xl mx-auto py-4">
    <div class="flex items-center gap-2 text-xs text-slate-400">
      <a href="/" class="hover:text-white transition-colors">홈</a>
      <span>&gt;</span>
      <a href="/tools" class="hover:text-white transition-colors">스마트 도구</a>
      <span>&gt;</span>
      <span class="text-indigo-400 font-semibold">마크다운(Markdown) 실시간 에디터</span>
    </div>

    <div class="text-center space-y-3">
      <div class="inline-flex items-center gap-2 px-4 py-1 rounded-full stitch-pill text-xs font-bold">
        <span>📝</span> <span>문서작성</span>
      </div>
      <h1 class="text-2xl sm:text-4xl font-extrabold text-white tracking-tight">
        마크다운(Markdown) 실시간 에디터
      </h1>
      <p class="text-slate-400 max-w-xl mx-auto text-xs sm:text-sm leading-relaxed">
        마크다운 문법으로 작성된 텍스트를 실시간으로 서식화된 HTML 뷰로 미리보고 HTML 코드를 복사합니다.
      </p>
    </div>

    <AdBanner type="leaderboard" label="스마트 비즈니스 &amp; 생산성 스폰서" />

    <MarkdownPreviewTool client:load />

    <AdBanner type="horizontal" className="my-8" />
  </div>
</Layout>
