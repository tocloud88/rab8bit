---
interface Props {
  leftSlotId?: string;
  rightSlotId?: string;
  client?: string;
}

const {
  leftSlotId = '1234567891',
  rightSlotId = '1234567892',
  client = 'ca-pub-XXXXXXXXXXXXXXXX'
} = Astro.props;
---

<!-- Widescreen Fixed Side Rail Ads (Visible on Display >= 1536px) -->
<aside aria-label="사이드 스폰서 광고" class="hidden 2xl:block pointer-events-auto">
  <!-- Left Side Rail Ad (Wide Skyscraper 160x600) -->
  <div 
    class="fixed top-24 z-30 w-[150px] transition-all duration-300 left-[max(16px,calc(50%-600px-165px))] group"
  >
    <div class="rounded-2xl overflow-hidden border border-indigo-500/25 bg-slate-900/80 dark:bg-slate-950/90 backdrop-blur-xl shadow-2xl flex flex-col justify-between">
      <!-- Ad Header Label -->
      <div class="flex items-center justify-between px-2.5 py-1 bg-slate-950/90 border-b border-slate-800 text-[9px] text-slate-500 select-none">
        <span class="px-1 py-0.2 rounded bg-indigo-500/20 text-indigo-400 font-bold text-[8px]">AD</span>
        <span>스폰서 추천</span>
      </div>

      <!-- Google AdSense Container & Mock Graphic -->
      <div class="p-1.5 flex-1 flex flex-col items-center justify-between text-center">
        <ins
          class="adsbygoogle w-full block"
          style="display:none"
          data-ad-client={client}
          data-ad-slot={leftSlotId}
          data-ad-format="vertical"
          data-full-width-responsive="false"
        ></ins>

        <a 
          href="/ai-tools"
          class="block w-full overflow-hidden rounded-xl transition-all duration-300 group-hover:scale-[1.02] border border-slate-800 hover:border-indigo-500/50 shadow-md"
          title="스폰서 추천 광고"
        >
          <img 
            src="/images/ads/mock-siderail-saas.png" 
            alt="AI SaaS 올인원 스폰서 광고" 
            loading="lazy" 
            class="w-full h-auto object-cover rounded-xl"
          />
        </a>
      </div>
    </div>
  </div>

  <!-- Right Side Rail Ad (Wide Skyscraper 160x600) -->
  <div 
    class="fixed top-24 z-30 w-[150px] transition-all duration-300 right-[max(16px,calc(50%-600px-165px))] group"
  >
    <div class="rounded-2xl overflow-hidden border border-indigo-500/25 bg-slate-900/80 dark:bg-slate-950/90 backdrop-blur-xl shadow-2xl flex flex-col justify-between">
      <!-- Ad Header Label -->
      <div class="flex items-center justify-between px-2.5 py-1 bg-slate-950/90 border-b border-slate-800 text-[9px] text-slate-500 select-none">
        <span class="px-1 py-0.2 rounded bg-purple-500/20 text-purple-400 font-bold text-[8px]">AD</span>
        <span>스폰서 추천</span>
      </div>

      <!-- Google AdSense Container & Mock Graphic -->
      <div class="p-1.5 flex-1 flex flex-col items-center justify-between text-center">
        <ins
          class="adsbygoogle w-full block"
          style="display:none"
          data-ad-client={client}
          data-ad-slot={rightSlotId}
          data-ad-format="vertical"
          data-full-width-responsive="false"
        ></ins>

        <a 
          href="/ai-tools"
          class="block w-full overflow-hidden rounded-xl transition-all duration-300 group-hover:scale-[1.02] border border-slate-800 hover:border-purple-500/50 shadow-md"
          title="스폰서 추천 광고"
        >
          <img 
            src="/images/ads/mock-siderail-saas.png" 
            alt="AI SaaS 올인원 스폰서 광고" 
            loading="lazy" 
            class="w-full h-auto object-cover rounded-xl"
          />
        </a>
      </div>
    </div>
  </div>
</aside>
